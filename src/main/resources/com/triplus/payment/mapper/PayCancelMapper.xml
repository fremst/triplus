<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.triplus.payment.mapper.PayCancelMapper">
    <insert id="insert" parameterType="com.triplus.payment.dto.PayCancelDto">
        insert into "PAYCANCEL" (CTID, TID, MSG, CANCELDATE, CANCELTIME)
        values (#{cTid}, #{tid}, #{msg}, #{cancelDate}, #{cancelTime})
    </insert>
</mapper>